services:
  db:
    image: pgvector/pgvector:pg16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: geolab
      POSTGRES_PASSWORD: geolab
      POSTGRES_DB: geolab_kb
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
